{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CodeLLDB: Launch and Debug (Final External)",
            "type": "lldb", // 💡 CodeLLDB 확장 사용
            "request": "launch",
            
            "program": "${workspaceFolder}/exec_file", // tasks.json이 생성한 실행 파일 경로
            "args": [],
            
            "cwd": "${workspaceFolder}", 
            
            // 💡 디버깅 전에 tasks.json의 빌드 작업을 호출
            "preLaunchTask": "CPP: Build Project (Final)", 
            
            // 💡 외부 터미널 사용 설정 (integratedTerminal 오류 및 콘솔 입출력 문제 해결)
            "terminal": "external", 
            
            // 💡 TERM 환경 변수 설정 (system("clear") / cls() 함수 정상 작동 보장)
            "env": {
                "TERM": "xterm-256color" 
            }
        }
    ]
}